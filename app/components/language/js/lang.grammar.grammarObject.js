autoSim.Grammar = function (id, variable, alphabet, production, startvariable) {
    var self = this;

    self.id = id;
    self.variable = variable;
    self.alphabet = alphabet;
    self.production = production;
    self.startvariable = startvariable;
    
};