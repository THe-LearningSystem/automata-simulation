autoSim.Grammar = function (id, variable, alphabet, startvariable) {
    var self = this;

    self.id = id;
    self.variable = variable;
    self.alphabet = alphabet;
    self.startvariable = startvariable;
    
};